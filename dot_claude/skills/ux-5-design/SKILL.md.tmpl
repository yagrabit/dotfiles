---
name: ux-5-design
description: "ロゴ生成（55スタイル）・CIPモックアップ・アイコン・ソーシャルフォト生成。Gemini AIを活用した総合デザインスキル"
user-invocable: true
allowed-tools: Bash, Read, Write, AskUserQuestion
---

## 言語

- ユーザーとのやりとりは日本語で行うこと
- 検索クエリやスクリプト実行時のキーワードは英語を使用すること（CSVデータが英語のため）
- 検索結果の説明・提案は日本語に翻訳して伝えること

以下のファイルを読み込み、その指示に従って動作してください:

{{ .chezmoi.homeDir }}/ghq/github.com/yagrabit/ui-ux-pro-max-skill/.claude/skills/design/SKILL.md

## 補足: スキル名のマッピング

参照先ドキュメント内のスキル名は以下のように読み替えてください:

- `ckm:design` → このスキル自身（ux-5-design）
- `ui-ux-pro-max` → `/ux-3-core`
- `ckm:ui-styling` → `/ux-4-styling`
- `ckm:design-system` → `/ux-2-tokens`
- `ckm:banner-design` → `/ux-6-banner`
- `ckm:brand` → `/ux-1-brand`
- `ckm:slides` → `/ux-7-slides`

## 補足: スクリプトパス

Python スクリプトの実行時は、以下のベースパスを使用してください:

{{ .chezmoi.homeDir }}/ghq/github.com/yagrabit/ui-ux-pro-max-skill/.claude/skills/design/scripts/

## 補足: 前提条件

- 環境変数 `GEMINI_API_KEY` が必要（mise で管理）
- Python パッケージ: `pip install google-genai pillow`（ロゴ/CIP生成時のみ）

## 補足: 利用不可のスキル

参照先で言及されている以下のスキルは未導入です。これらが必要な機能は手動で代替してください:
- `ai-artist`, `ai-multimodal`, `chrome-devtools`
