#!/bin/bash
# dotfilesリポジトリのgitフックを設定

set -eufo pipefail

DOTFILES_DIR="{{ .chezmoi.sourceDir }}"

if [ -d "$DOTFILES_DIR/.git" ]; then
  cd "$DOTFILES_DIR"

  # .hooksディレクトリをgitフック格納先に設定
  if [ -d ".hooks" ]; then
    git config core.hooksPath .hooks
    echo "gitフックを設定しました"
  fi
fi
